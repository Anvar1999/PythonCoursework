class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = 121
        j = str(x)
        t = len(j)
        if x[0] == t[0]:
            return True
        else:
            return False
